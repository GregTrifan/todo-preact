import { Header, HeaderName } from "carbon-components-react";

const Navbar = () => {
  return (
    <Header>
      <HeaderName prefix="Lorem">Ipsum</HeaderName>
    </Header>
  );
};
export default Navbar;
